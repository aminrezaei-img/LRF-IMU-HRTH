| No. | Script | Summary | How to run | Status |
|-----|--------|---------|------------|--------|
| 1 | `VAE/Run_VAE_Pretraings.ps1` | Trains one VAE per LOSO fold (12 subjects). Outputs checkpoints to `Results/model_weights/vae_weights/6CH/full/subject_XX/vae_s4_z48.pt`. | `.\VAE\Run_VAE_Pretraings.ps1 -IdealDir "D:/PAPER2/SALVAGED_PARTS/ideal_logs" -Subjects @(1,2,3,5,8,9,10,11,12,13,14,16)` | ✅ |
| 2 | `1_train_flow.ps1` | Trains one Rectified Flow model per LOSO fold using the VAE checkpoints from step 1. Outputs flow weights to `Results/model_weights/flow_weights/6CH/full/subject_XX/`. | `.\1_train_flow.ps1` | ✅ |
| 3 | `training_progression.py` | Computes FLOPs, wall-clock summaries, and 4×3 training curve plots for both VAE and Flow. Outputs to `Results/training_progression/`. | `python training_progression.py --module all` | ✅ |
| 4 | `training_curves_figure_6.py` | Per-subject 1×2 training curve panels (VAE left, Flow right) for all 12 LOSO folds. Outputs PDF+PNG per subject to `Results/training_curve_plots/`. | `python training_curves_figure_6.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --stride 4 --zdim 48 --vae-ckpt-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --flow-ckpt-template "Results/model_weights/flow_weights/6CH/full/subject_{subject:02d}/flow_unet_best.pt"` | ✅ |
| 5 | `detailed_analysis.py` | Generative model quality audit: data integrity, memorization detection, sample diversity. Runs all 12 LOSO folds. Outputs to `Results/detailed_analysis/6CH/subject_XX/`. | `PYTHONIOENCODING=utf-8 python detailed_analysis.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --stride 4 --zdim 48 --num-steps 10 --data-root "D:/PAPER2/SALVAGED_PARTS/ideal_logs" --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --flow-template "Results/model_weights/flow_weights/6CH/full/subject_{subject:02d}/flow_unet_best.pt"` | ✅ |
| 6 | `5_flow_trajectory_figure.py` | Flow denoising trajectory visualization (Fig 4). Runs all 12 LOSO folds, class Cycling. Outputs `Fig4_Flow_Trajectory_6Channel_steps10.pdf/.png` to `Results/flow_trajectory/subject_XX/`. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python .\5_flow_trajectory_figure.py --base-run-name "6CH/full" --subject $s --vae-root "Results/model_weights/vae_weights/6CH/full" --stride 4 --zdim 48 --class-id 3 --class-name "Cycling" --num-steps 10 --snapshots 0 2 4 6 8 10 --noise-scale 0.5 --viz-std-match 1 }` | ✅ |
| 7 | `2_generate_synthetic_all_folds.ps1` | Generates and caches synthetic NPZ files for all 12 LOSO folds (required before steps 8–21). Outputs to `Results/synthetic_weights/6CH/full/subject_XX/steps10_synthetic.npz`. | `.\2_generate_synthetic_all_folds.ps1` | ✅ |
| 8 | `6_phyiscs_plausibility.py` | Physical plausibility analysis (Fig 3 + tables). Loads cached synthetic NPZs. Outputs per-subject figures and CSVs to `Results/physics_plausibility/subject_XX/`, pooled table to `Results/physics_plausibility/`. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 6_phyiscs_plausibility.py --run-name "6CH/full" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --stride 4 --zdim 48 --num-steps 10 --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --synth-root "Results/synthetic_weights/6CH/full"` | ✅ |
| 9 | `6_1_physics_summary_across_folds.py` | Merged all-folds physics summary (supersedes 6_1/6_2/6_3). Aggregates per-subject CSVs, computes real-real baseline, generates Fig 3 (PDF+PNG), summary_metrics.csv, physics_metrics_per_fold.csv, .tex tables, real_real_corr_baseline files. All outputs to `Results/physics_plausibility/allfolds/`. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 6_1_physics_summary_across_folds.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --num-steps 10 --synth-root "Results/synthetic_weights/6CH/full" --physics-root "Results/physics_plausibility" --out-dir "Results/physics_plausibility/allfolds"` | ✅ |
| 10 | `7_cluster_analysis.py` | DTW k-means structural consistency analysis per LOSO fold. Uses `--cluster-pick most_mixed` to match manuscript. Outputs per-subject figures, CSVs, and cluster_results.json to `Results/cluster_analysis/subject_XX/`. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; $env:MPLBACKEND="Agg"; foreach ($sid in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python 7_cluster_analysis.py --run-name "6CH/full" --test-subject $sid --num-steps 10 --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --synth-root "Results/synthetic_weights/6CH/full" --target-name "Cycling" --n-clusters 20 --max-samples 500 --seed 42 --cluster-pick most_mixed }` | ✅ |
| 11 | `7_1_summarize_cluster_analysis_across_folds.py` | Aggregates per-subject cluster_results.json files into summary CSVs across all LOSO folds. Outputs cluster_summary_per_fold.csv and cluster_summary_across_folds.csv to `Results/cluster_analysis/allfolds/`. | `python 7_1_summarize_cluster_analysis_across_folds.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --cluster-root "Results/cluster_analysis"` | ✅ |
| 12 | `9_psd_freq_analysis_data_efficiency.py` | Per-subject Welch PSD comparison (real vs synthetic) and data efficiency augmentation curve. Covers manuscript §Spectral fidelity (PSD) and §Data efficiency across training fractions. Outputs figures and augmentation_results_6ch.csv to `Results/psd_analysis/subject_XX/PSD_FRQ_Analysis/`. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; $env:MPLBACKEND="Agg"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python 9_psd_freq_analysis_data_efficiency.py --run-name "6CH/full" --test-subject $s --num-steps 10 --synth-root "Results/synthetic_weights/6CH/full" }` | ✅ |
| 13 | `9_1_summarize_psd_efficiency_across_folds.py` | Aggregates PSD and data efficiency across all LOSO folds. Covers manuscript §Spectral fidelity (PSD) and §Data efficiency across training fractions. Outputs psd_summary_real/syn_6ch.csv, psd_spectral_stats_6ch.csv (log-PSD correlations, band-power ratios, std ratios), data_efficiency_across_folds.csv, data_efficiency_per_fold.csv, and figures to `Results/psd_analysis/allfolds/`. Note: the data efficiency table in the manuscript (tab:data_efficiency) is manually typeset from data_efficiency_across_folds.csv — no auto-generated .tex. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 9_1_summarize_psd_efficiency_across_folds.py --run-name "6CH/full" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --num-steps 10 --synth-root "Results/synthetic_weights/6CH/full"` | ✅ |
| 14 | `11_combined_manifold.py` | Per-subject distributional similarity: UMAP projections + MMD² + C2ST in both signal and latent space (Fig: `Fig_Combined_Manifold.pdf`). **Requires step 7 (synthetic cache).** Outputs to `Results/distinctness_and_population_coverage/6CH/full/subject_XX/Combined_Manifold/`. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python 11_combined_manifold.py --run-name "6CH/full" --test-subject $s --stride 4 --zdim 48 --num-steps 10 --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --synth-root "Results/synthetic_weights/6CH/full" }` | ✅ |
| 15 | `11_1_summarize_combined_manifold_across_folds.py` | All-folds combined manifold summary. **Requires step 14.** Pools embeddings across all LOSO folds, generates combined UMAP figure (`Fig_Combined_Manifold_ALL.pdf`), `combined_manifold_metrics_per_fold.csv`, `combined_manifold_metrics_across_folds.csv`. Outputs to `Results/distinctness_and_population_coverage/6CH/full/Combined_Manifold_ALL/`. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 11_1_summarize_combined_manifold_across_folds.py --run-name "6CH/full" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --stride 4 --zdim 48 --num-steps 10 --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt" --synth-root "Results/synthetic_weights/6CH/full"` | ✅ |
| 16 | `10_coverage_population_analysis.py` | Per-subject population coverage and novelty analysis: PCA convex hulls, nearest-neighbour distances, coverage ratio, per-class metrics. **Requires step 7.** Outputs `Fig_Population_Spread.png`, `Table3_Novelty_Metrics.csv`, `Table_PerClass_Metrics_Raw.csv` to `Results/distinctness_and_population_coverage/6CH/full/subject_XX/Distinctnes_population_coverage/`. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python 10_coverage_population_analysis.py --run-name "6CH/full" --test-subject $s --num-steps 10 --synth-root "Results/synthetic_weights/6CH/full" }` | ✅ |
| 17 | `10_1_summarize_coverage_across_folds.py` | Aggregates per-subject coverage CSVs across all LOSO folds. **Requires step 16.** Outputs `coverage_metrics_per_fold.csv`, `coverage_metrics_across_folds.csv`, `coverage_metrics_across_folds.tex` to `Results/distinctness_and_population_coverage/6CH/full/Distinctnes_population_coverage_all/`. | `python 10_1_summarize_coverage_across_folds.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --per-class` | ✅ |
| 18 | `10_2_coverage_summary_all_folds_v6.py` | All-folds population coverage figure (v6): re-runs coverage computation from synth cache across all folds, generates pooled PCA + KDE plot (`Fig_Coverage_Summary_AllFolds.pdf`) and summary CSVs. **Requires step 7 (synthetic cache). Independent of step 16–17.** Outputs to `Results/distinctness_and_population_coverage/6CH/full/Coverage_Summary_updated/`. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 10_2_coverage_summary_all_folds_v6.py --run-name "6CH/full" --num-steps 10 --subjects 1 2 3 5 8 9 10 11 12 13 14 16` | ✅ |
| 19 | `run_tstr_all_subjects.ps1` | TSTR downstream classification (RF + CNN) for all 12 LOSO folds using cached synthetic. **Requires step 7.** Internally calls `8_tstr_classification_figure_org_PATCHED_v3.py` once per subject per classifier (24 calls total). Outputs 6 files per subject per classifier to `Results/tstr_classification/6CH/full/subject_XX/TSTR_Classification_{CNN\|RF}/` (Fig5 PDF+PNG, Classification_Results.csv, PerClass_Report.csv, AllScenarios_Report.csv, Confusion_norm.csv). **Must run before step 20.** | `.\run_tstr_all_subjects.ps1` | ✅ |
| 20 | `8_1_summarize_tstr_classification_across_folds.py` | Aggregates TSTR classification results across all LOSO folds. **Requires step 19.** Reads from `Results/tstr_classification/6CH/full/subject_XX/`. Outputs per_fold, summary, per_class, delta, aug_summary, confusion_mean CSVs and combined figure to `Results/tstr_classification/6CH/full/allfolds/`. | `python 8_1_summarize_tstr_classification_across_folds.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --classifier both` | ✅ |
| 21 | `13_classification_violin_confusion_PATCHED.py` | Fig 5 — violin + confusion matrices for augmentation under limited data (2 real samples/class across all 12 LOSO folds). **Requires step 7 (synthetic cache).** Outputs `Fig_5_violin_Confusion_Matrices.pdf/.png` + `classification_violin_plot_df.csv` + `classification_violin_confusion_cache.npz` to `Results/distinctness_and_population_coverage/6CH/full/Classification_violin_all/`. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; python 13_classification_violin_confusion_PATCHED.py --run-name "6CH/full" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --num-steps 10 --synth-root "Results/synthetic_weights/6CH/full"` | ✅ |
| 22 | `windowing_sensitivity_grid_3x3.ps1` + `window_grid_aug_quality.py` | **Independent of steps 1–21.** Trains 9 VAE+Flow configurations for the 3×3 window×stride sensitivity grid (WIN∈{80,160,240} × HOP∈{20,40,80}), runs LOSO TSTR evaluation per configuration, then computes augmentation Macro-F1 and PSD quality post-hoc. Covers manuscript §Robustness — Windowing sensitivity (tab:windowing_sensitivity_summary). VAE weights → `Results/sensitivity_grid_analysis/VAE/<CONFIG>/subject_XX/`. Flow TSTR results → `Results/sensitivity_grid_analysis/FLOW/<CONFIG>/subject_XX/`. Aggregate summary files → `Results/sensitivity_grid_analysis/summary/` (`window_grid_aug_quality_per_fold.csv`, `window_grid_aug_quality_summary.csv`, `window_grid_aug_quality_summary.tex`, `windowing_sensitivity_summary.json`, `windowing_aug_quality_manuscript_insertions.tex`). | Phase 1+2+3 (train + TSTR): `.\windowing_sensitivity_grid_3x3.ps1`; Post-hoc quality: `python window_grid_aug_quality.py` | ✅ |
| 23 | `vae_ablation.ps1` + `vae_ablation_loso.py` | **Requires step 1 VAE weights only. Independent of steps 2–22.** VAE-only component ablation: generates synthetic windows by sampling from per-class Gaussian fitted to VAE posterior means (no Rectified Flow), then runs RF LOSO TSTR. Covers manuscript §Robustness — VAE-only ablation (tab:vae_ablation_summary). Outputs per-subject `tstr_results.json` to `Results/vae_ablation/subject_XX/`, summary CSV to `Results/vae_ablation/vae_ablation.csv`. Comparison table (VAE-only vs LRF-IMU) printed to console via `sensitivity_summary.py`. | `.\vae_ablation.ps1` | ✅ |
| 24 | `VAE/Run_VAE_Pretraings.ps1 -AblationAccOnly` | **3CH ablation — Step A: VAE pretraining.** Trains one VAE per LOSO fold using only the 3 accelerometer channels (sets `ABLATION_ACC_ONLY=1`). Outputs checkpoints to `Results/model_weights/vae_weights/3CH/ablation/subject_XX/vae_s4_z48.pt`. Weights already exist; only re-run to reproduce from scratch. | `.\VAE\Run_VAE_Pretraings.ps1 -AblationAccOnly -IdealDir "D:/PAPER2/SALVAGED_PARTS/ideal_logs" -Subjects @(1,2,3,5,8,9,10,11,12,13,14,16)` | ✅ |
| 25 | `3_run_full_losocv.py --ablation` | **3CH ablation — Step B: Flow training.** Trains one Rectified Flow model per LOSO fold in 3-channel mode. This is the combined train+TSTR pipeline; the TSTR output here goes to `Results/model_weights/flow_weights/3CH/ablation/subject_XX/tstr_results.json` (basic metrics only). **Internal defaults are stale** (`LOSO_VAE_ROOT = "runs/LOSO_VAE_CLEAN"`); override via CLI as shown. Flow weights output to `Results/model_weights/flow_weights/3CH/ablation/subject_XX/`. Weights already exist; only re-run to reproduce from scratch. | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; python 3_run_full_losocv.py --ablation --vae-template "Results/model_weights/vae_weights/3CH/ablation/subject_{subject:02d}/vae_s4_z48.pt" --base-run-name "3CH/ablation" --subjects 1 2 3 5 8 9 10 11 12 13 14 16` | ✅ |
| 26 | `2_generate_synthetic_all_folds.ps1` (3CH variant) | **3CH ablation — Step C: Synthetic cache generation.** Generates and caches NPZ synthetic data for all 12 folds using the 3CH flow weights. Required before steps 27 and 30. Outputs to `Results/synthetic_weights/3CH/ablation/subject_XX/steps10_synthetic.npz`. Run by invoking the generator script directly with 3CH paths (no dedicated PS1 for 3CH; set env vars manually or copy the PS1 and change `$baseRunName`, `$vaeRoot`, `$synthCacheRoot` to 3CH equivalents). | `$env:RUNS_ROOT="Results/model_weights/flow_weights"; $env:TSTR_GENERATE_ONLY="1"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { $sf=$s.ToString("D2"); $env:RUN_NAME="3CH/ablation/subject_$sf"; $env:VAE_CKPT="Results/model_weights/vae_weights/3CH/ablation/subject_$sf/vae_s4_z48.pt"; $env:REALDISP_TEST_SUBJECT="$s"; $env:TSTR_NUM_STEPS="10"; $env:TSTR_SYNTH_NPZ_OUT="Results/synthetic_weights/3CH/ablation/subject_$sf/steps10_synthetic.npz"; New-Item -Force -ItemType Directory "Results/synthetic_weights/3CH/ablation/subject_$sf" | Out-Null; python 2_generate_synthetic_only.py }` | ✅ |
| 27 | `10_coverage_population_analysis.py --ablation` | **3CH ablation — Step D: Per-subject population coverage.** Runs PCA convex hull, nearest-neighbour distances, coverage ratio analysis for each of the 12 LOSO folds using 3CH synthetic data. **Requires step 26 (3CH synth cache).** Outputs `Table3_Novelty_Metrics.csv`, `Fig_Population_Spread.png`, per-class CSVs to `Results/distinctness_and_population_coverage/3CH/ablation/subject_XX/Distinctnes_population_coverage/`. This CSV is the direct input to step 29 (`aggregate_generative_quality_6v3.py`). | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python 10_coverage_population_analysis.py --run-name "3CH/ablation" --test-subject $s --num-steps 10 --synth-root "Results/synthetic_weights/3CH/ablation" --ablation }` | ✅ |
| 28 | `10_1_summarize_coverage_across_folds.py` (3CH) | **3CH ablation — Step E: Coverage all-folds summary.** Aggregates per-subject `Table3_Novelty_Metrics.csv` files across all 12 folds for the 3CH ablation. **Requires step 27.** Outputs `coverage_metrics_per_fold.csv`, `coverage_metrics_across_folds.csv`, `.tex` table to `Results/distinctness_and_population_coverage/3CH/ablation/Distinctnes_population_coverage_all/`. | `python 10_1_summarize_coverage_across_folds.py --runs-root "Results/distinctness_and_population_coverage/3CH/ablation" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --per-class` | ✅ |
| 29 | `aggregate_generative_quality_6v3.py` | **Produces `tab:gen_quality_6v3` in the manuscript (§Sensor reduction — generative quality comparison table).** Reads per-subject `Table3_Novelty_Metrics.csv` from both 6CH (`Results/distinctness_and_population_coverage/6CH/full/subject_XX/Distinctnes_population_coverage/`) and 3CH (`Results/distinctness_and_population_coverage/3CH/ablation/subject_XX/Distinctnes_population_coverage/`). Computes mean ± SD across 12 LOSO folds for diversity score, min novelty distance, coverage ratio, and PCA area ratio. Computes retention ratio (3CH/6CH × 100%). **Requires steps 17 (6CH coverage) and 28 (3CH coverage).** Outputs `generative_quality_6v3_comparison.csv` + `generative_quality_6v3_table.tex` to `Results/distinctness_and_population_coverage/ablation_comparison/`. | `python aggregate_generative_quality_6v3.py` | ✅ |
| 30 | `8_tstr_classification_figure_org_PATCHED_v3.py --ablation` | **3CH ablation — Step F: TSTR classification figures per subject.** Runs RF + CNN TSTR evaluation using the 3CH synthetic cache, producing classification figures and CSVs. **Requires step 26 (3CH synth cache).** Outputs to `Results/tstr_classification/3CH/ablation/subject_XX/TSTR_Classification_{CNN\|RF}_ablation/` (6 files per subject per classifier). No PS1 orchestrator exists for 3CH; invoke manually per subject. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; $env:MPLBACKEND="Agg"; foreach ($job in @("cnn","rf")) { foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { $sf=$s.ToString("D2"); python 8_tstr_classification_figure_org_PATCHED_v3.py --run-name "3CH/ablation" --test-subject $s --num-steps 10 --synth-root "Results/synthetic_weights/3CH/ablation" --ablation } }` | ✅ |
| 31 | `8_1_summarize_tstr_classification_across_folds.py` (3CH) | **3CH ablation — Step G: TSTR all-folds summary.** Aggregates per-subject TSTR classification CSVs across all 12 folds for the 3CH ablation. **Requires step 30.** Outputs `tstr_classification_rf_summary.csv`, `tstr_classification_cnn_summary.csv`, per-fold CSVs, combined figure to `Results/tstr_classification/3CH/ablation/allfolds/`. These CSVs are the direct input to step 32. | `python 8_1_summarize_tstr_classification_across_folds.py --runs-root "Results/tstr_classification/3CH/ablation" --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --classifier both` | ✅ |
| 32 | `make_tstr_table_6v3.py` | **Produces `tab:tstr_utility_6v3` in the manuscript (§Sensor reduction — TSTR utility comparison table).** Reads `tstr_classification_rf_summary.csv` and `tstr_classification_cnn_summary.csv` from both 6CH (`Results/tstr_classification/6CH/full/allfolds/`) and 3CH (`Results/tstr_classification/3CH/ablation/allfolds/`). Computes TSTR macro-F1 mean ± SD across 12 folds for RF and CNN classifiers, and the 3CH retention ratio (3CH/6CH × 100%). **Requires steps 20 (6CH TSTR summary) and 31 (3CH TSTR summary).** Outputs comparison CSV + `.tex` table. | `python make_tstr_table_6v3.py --dir-6ch "Results/tstr_classification/6CH/full/allfolds" --dir-3ch "Results/tstr_classification/3CH/ablation/allfolds"` | ✅ |
| 33 | `3_run_loso_evaluation.ps1` | **Produces the per-fold `tstr_results.json` files in `Results/loso/` used to build the main LOSO classification table.** Runs `TSTR.py` with both RF and CNN classifiers for all 12 LOSO folds using the pre-generated synthetic NPZ cache. Outputs `tstr_results.json`, `train_summary.json`, `flow_history.json`, `run_manifest.json` to `Results/loso/Random_forest_classifier/6CH/full/subject_XX/` and `Results/loso/CNN/6CH/full/subject_XX/`. **Requires step 7 (synthetic cache).** Results already exist; only re-run to reproduce from scratch. | `.\3_run_loso_evaluation.ps1` | ✅ |
| 34 | `4_1_make_loso_csv_summaries.py` | Aggregates all per-fold `tstr_results.json` files from `Results/loso/` into two summary CSVs: `loso_summary_by_classifier.csv` (mean ± SD per metric per classifier) and `loso_cnn_minus_rf_summary.csv` (paired CNN − RF delta per metric). **Requires step 33.** Both CSVs are direct inputs to step 35. Outputs to `Results/loso/`. | `python 4_1_make_loso_csv_summaries.py --root "Results/loso" --out-summary "Results/loso/loso_summary_by_classifier.csv" --out-delta-summary "Results/loso/loso_cnn_minus_rf_summary.csv"` | ✅ |
| 35 | `4_2_make_loso_latex_tables.py` | **Produces `table_loso_overall.tex` and `table_loso_perclass.tex` in `Results/loso/`** — the main LOSO classification results table (overall accuracy, macro-F1, retention) and the per-activity F1 table. **Requires step 34.** | `python 4_2_make_loso_latex_tables.py --summary "Results/loso/loso_summary_by_classifier.csv" --delta "Results/loso/loso_cnn_minus_rf_summary.csv" --out-overall "Results/loso/table_loso_overall.tex" --out-perclass "Results/loso/table_loso_perclass.tex" --n-folds 12 --decimals 3` | ✅ |
| 36 | `4_3_make_efficiency_table.py` | **Produces `table_efficiency.tex` in `Results/loso/`** — the training and generation efficiency table (wall-clock time, params, synthesis latency). Reads `train_summary.json` and `tstr_results.json` from `Results/loso/`. **Requires step 33.** | `python 4_3_make_efficiency_table.py --root "Results/loso" --out "Results/loso/table_efficiency.tex" --caption "\textbf{Training and generation efficiency} (mean \$\pm\$ SD) across 12 LOSO folds." --label "tab:efficiency" --time-unit min --preset minimal --collapse-identical` | ✅ |
| 37 | `12_privacy_memorization_audit.py` | **Produces the privacy and memorisation audit numbers cited in §Distinctness and novelty** (AUC, P1/P5 proximity rates, syn-to-train distances, reconstruction attack success rate). Runs all 6 audit levels (exact duplicates, near-duplicates, train-baseline proximity, membership inference, latent optimisation) for all 12 LOSO folds. **Requires step 7 (synthetic cache).** Outputs per-subject `privacy_audit_results.json`, `privacy_audit_summary.csv`, `privacy_audit_table.tex`, `privacy_audit_figure.pdf` to `Results/privacy_audit/subject_XX/`; all-folds `privacy_audit_all_folds.csv` and `privacy_audit_summary.tex` to `Results/privacy_audit/summary/`. Results already exist; only re-run to reproduce from scratch. | `python 12_privacy_memorization_audit.py --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --synth-cache-root "Results/synthetic_weights/6CH/full" --output-dir "Results/privacy_audit"` | ✅ |
| 38 | `12_2_extract_reconstruction_stats.py` | Reads per-subject `privacy_audit_results.json` files and extracts Level 6 (optimisation-based reconstruction attack) statistics. **Requires step 37.** Outputs `reconstruction_attack_summary.csv` and `reconstruction_attack_latex.txt` to `Results/privacy_audit/summary/`. The LaTeX snippet matches the reconstruction attack paragraph in the manuscript verbatim. | `python 12_2_extract_reconstruction_stats.py --audit-dir "Results/privacy_audit"` | ✅ |
| 39 | `12_1_summarize_privacy_audit.py` | Reads per-subject `privacy_audit_results.json` files and produces a pooled summary figure across all 12 LOSO folds. **Requires step 37.** Outputs `privacy_audit_summary_figure.pdf` to `Results/privacy_audit/summary/`. Figure is used for inspection; not embedded in the manuscript. | `python 12_1_summarize_privacy_audit.py` | ✅ |
| 40 | `membership_inference_holdout.py train` | **Phase 1 of 3 — Membership Inference Holdout.** Retrains one Rectified Flow model per LOSO fold with 10% of the training windows held out. Requires VAE weights from step 1 only (independent of main flow weights). Outputs `flow_unet_best.pt`, `flow_unet_latest.pt`, `flow_history.json`, `train_summary.json`, `mi_holdout_indices.npz`, `mi_holdout_split.json`, `synthetic_cache_mi.npz` per subject to `Results/membership_inference/w/mi10m/subject_XX/`. Results already exist; only re-run to reproduce from scratch. | `$env:REALDISP_IDEAL_DIR="D:\PAPER2\SALVAGED_PARTS\ideal_logs"; $env:MI_HOLDOUT_FRAC="0.10"; $env:MI_HOLDOUT_SEED="42"; python membership_inference_holdout.py train --subjects 1 2 3 5 8 9 10 11 12 13 14 16 --base-run-name "mi10m" --stride 4 --zdim 48 --vae-template "Results/model_weights/vae_weights/6CH/full/subject_{subject:02d}/vae_s{stride}_z{dim}.pt"` | ✅ |
| 41 | `membership_inference_holdout.py eval` | **Phase 2 of 3 — Membership Inference Holdout.** Evaluates ROC-AUC for each subject using the holdout flow from step 40 vs. the held-out windows. Run once per subject (loop over all 12). **Requires step 40.** Outputs `mi_auc_subjectXX.json` to `Results/membership_inference/r/`. Results already exist; only re-run to reproduce from scratch. | `foreach ($s in @(1,2,3,5,8,9,10,11,12,13,14,16)) { python membership_inference_holdout.py eval --test-subject $s }` | ✅ |
| 42 | `membership_inference_holdout.py summarize` | **Phase 3 of 3 — Membership Inference Holdout.** **Produces the membership inference ROC-AUC numbers cited in §Privacy and memorisation** (mean AUC 0.495±0.020, max fold 0.520, 12/12 folds below 0.55). **Requires step 41.** Outputs `merged_fulltrain_summary.csv` and `merged_fulltrain_summary.tex` to `Results/membership_inference/r/`. | `python membership_inference_holdout.py summarize` | ✅ |
